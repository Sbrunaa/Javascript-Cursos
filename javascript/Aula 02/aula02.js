//tipos de dados(string, number, booleano e array), tipos de dados(string, number, booleano e array) e Operadores

// tipos de dados(string, number, booleano e array)
const name = "value";

var Name = "value";

// e tipos de dados(string, number, booleano e array)
var empty = '';

var number = 1;

var True = true;
var False = false;

var objectList = {
    day: '10',
    month: 'january',
    year: '2026'
}

//Operadores
var valueA = 10;
var valueB = 20;

var sum = valueA + valueB; // resultado = 30

var subtraction = valueA - valueB; // resultado = -10

var multiplication = valueA * valueB; // resultado = 200

var division = valueA / valueB; // resultado = 0.5

//Operadores incremento e decremento

var value1 = 100;

value1++; // resultado = 101

value1--; // resultado = 99

var value2 = 100;

value += 10; //resultado = 110

value -= 10; // resultado = 90

//Pegar o resto, que dar para rerificar se um número é impar ou par

var valuePar = 8;
var valueImpar = 7;

valorPar % 2; // resultado = 0 ou seja é par
valorImpar % 2; // resultado = 1 ou seja é impar